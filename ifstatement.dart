// a code implementing a dart if statement// This is a simple Dart program that demonstrates the use of an if statement

void main() {
  // Initialize and declare an integer variable
  int x;

  // Assign a value to the variable
  x = 10;

  // Use an if statement to check if the variable is greater than 5
  if (x > 5) {
    print("x is greater than 5");
  }
}
